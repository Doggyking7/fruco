<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Truco Venezolano</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2c3e50;
            color: white;
            text-align: center;
            margin: 0;
            padding: 0;
            overflow: hidden;
            height: 100vh;
        }
        .game-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            box-sizing: border-box;
            height: 100%;
            display: flex;
            flex-direction: column;
        }
        .table {
            background-color: #006400; /* Verde oscuro */
            border: 5px solid #004d00;
            box-shadow: 0 0 10px rgba(0,0,0,0.5);
            padding: 20px;
            border-radius: 10px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        .player-hand, .opponent-hand, .played-cards {
            display: flex;
            justify-content: center;
            margin: 10px 0;
            min-height: 140px; /* Espacio suficiente para las cartas */
        }
        .card {
            width: 80px;
            height: 120px;
            background-color: white;
            color: black;
            margin: 0 10px;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            cursor: pointer;
        }
        .card.selected {
            border: 2px solid yellow;
        }
        .actions button {
            margin: 10px;
            padding: 10px 20px;
            font-size: 16px;
        }
        .score p {
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="game-container">
        <h1>Truco Venezolano</h1>
        <div class="table">
            <div class="opponent-hand" id="opponent-hand"></div>
            <div class="played-cards" id="played-cards"></div>
            <div class="player-hand" id="player-hand"></div>
        </div>
        <div class="actions">
            <button id="play-card">Jugar Carta</button>
            <button id="truco">Truco</button>
            <button id="envido">Envido</button>
        </div>
        <div class="score">
            <p>Puntuación Jugador: <span id="player-score">0</span></p>
            <p>Puntuación Oponente: <span id="opponent-score">0</span></p>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const palos = ['oro', 'copa', 'espada', 'basto'];
            const valores = [1, 2, 3, 4, 5, 6, 7, 10, 11, 12];

            function crearBaraja() {
                const baraja = [];
                palos.forEach(palo => {
                    valores.forEach(valor => {
                        baraja.push({ valor, palo });
                    });
                });
                return baraja;
            }

            function mezclarBaraja(baraja) {
                for (let i = baraja.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [baraja[i], baraja[j]] = [baraja[j], baraja[i]];
                }
                return baraja;
            }

            function repartirCartas(baraja) {
                const jugador = baraja.splice(0, 3);
                const oponente = baraja.splice(0, 3);
                return { jugador, oponente, baraja };
            }

            function renderizarCartas(mano, elementoId, ocultas = false) {
                const elemento = document.getElementById(elementoId);
                if (!elemento) {
                    console.error(`Elemento con ID ${elementoId} no encontrado.`);
                    return;
                }
                elemento.innerHTML = '';
                if (!mano || mano.length === 0) {
                    console.log(`Mano vacía para ${elementoId}`);
                    return;
                }
                mano.forEach((carta, index) => {
                    const cartaDiv = document.createElement('div');
                    cartaDiv.classList.add('card');
                    cartaDiv.dataset.index = index;
                    cartaDiv.textContent = ocultas ? '??' : `${carta.valor} de ${carta.palo}`;
                    elemento.appendChild(cartaDiv);
                });
            }

            let baraja = crearBaraja();
            console.log('Baraja creada con', baraja.length, 'cartas');
            baraja = mezclarBaraja(baraja);
            let { jugador, oponente, barajaRestante } = repartirCartas(baraja);
            console.log('Mano del jugador:', jugador);
            console.log('Mano del oponente:', oponente);

            renderizarCartas(jugador, 'player-hand');
            renderizarCartas(oponente, 'opponent-hand', true);

            // Verificar contenido después de renderizar
            console.log('Contenido de player-hand:', document.getElementById('player-hand').innerHTML);
            console.log('Contenido de opponent-hand:', document.getElementById('opponent-hand').innerHTML);
        });
    </script>
</body>
</html>